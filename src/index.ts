export { default as sharpify } from './sharpify'
export { default as isImage } from './is-image'

// explicitly export it, such that it's visible outside of the package
export type { SharpifyParameters } from './types'
